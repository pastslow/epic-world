import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
   appId: 'com.example.app',
   appName: 'epic-world',
   webDir: 'dist/epic-world/browser/',
};

export default config;
